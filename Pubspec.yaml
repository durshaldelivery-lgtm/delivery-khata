name: delivery_khata
description: "A delivery logistics and localized ledger application."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  
  # Localized Data Management Configurations
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Contact Synchronization Pipelines
  flutter_contacts: ^1.1.9
  permission_handler: ^11.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
